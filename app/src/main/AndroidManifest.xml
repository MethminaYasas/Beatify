<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:label="Beatify"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Splash screen is launcher, so exported must be true -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity to show songs and theme button -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Music player screen -->
        <activity
            android:name=".PlayerActivity"
            android:exported="false" />

        <!-- Theme customization screen -->
        <activity
            android:name=".ThemeCustomizationActivity"
            android:exported="false" />

        <activity android:name=".ArtistsActivity" />
        <activity android:name=".PlaylistsActivity" />


    </application>

</manifest>
